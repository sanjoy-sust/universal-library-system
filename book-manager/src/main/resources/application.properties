spring.application.name=book-manager
spring.datasource.url=jdbc:mysql://localhost:3306/book-manager
spring.datasource.username=root
spring.datasource.password=root
server.port=0
eureka.instance.leaseRenewalIntervalInSeconds = 10
eureka.instance.metadataMap.instanceId=${vcap.application.instance_id:${spring.application.name}:${spring.application.instance_id:${random.value}}}

lombok.equalsAndHashCode.callSuper = call

eureka.instance.instanceId=${spring.application.name}:${spring.application.instance_id:${random.value}}

eureka.client.registryFetchIntervalSeconds=5
eureka.client.serviceUrl.defaultZone=http://localhost:10210/eureka
spring.cloud.service-registry.auto-registration.enabled=true
eureka.client.enabled=true
1eureka.client.serviceUrl.registerWithEureka=true